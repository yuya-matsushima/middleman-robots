language: ruby
sudo: false
cache: bundler
before_script:
  - bundle update
rvm:
  - ruby-head
  - 3.0.1
  - 2.7.4
  - 2.6.8
os:
  - linux
matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
script:
  - bundle exec rubocop
  - bundle exec rake test
cache: bundler
env: TEST=true
